<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <title>Admin — @ViewData["Title"] — Sterlin Glams</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/app.css" asp-append-version="true" />
    <link rel="icon" type="image/png" sizes="32x32" href="~/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="~/favicon-16.png" />
    <link rel="apple-touch-icon" href="~/apple-touch-icon.png" />
</head>
<body class="bg-neutral-50 text-neutral-900 font-inter antialiased">

<div class="flex h-screen overflow-hidden">

    <!-- ─── Sidebar ──────────────────────────────────────────────────────── -->
    <aside class="w-64 bg-neutral-900 text-white flex flex-col flex-shrink-0 overflow-y-auto">

        <!-- Logo -->
        <div class="px-6 py-6 border-b border-neutral-800">
            <span class="font-cormorant text-xl font-light tracking-[0.2em] uppercase">Sterlin Glams</span>
            <p class="text-[10px] text-neutral-500 tracking-widest uppercase mt-0.5">Admin Console</p>
        </div>

        <!-- Nav -->
        <nav class="flex-1 px-3 py-6 space-y-1">
            @{
                var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
                var allowedSections = ViewData["AllowedSections"] as HashSet<string> ?? new HashSet<string>();
                var isFullAdmin = ViewData["IsFullAdmin"] as bool? ?? false;
            }

            @foreach (var item in SterlingLams.Web.Areas.Admin.AdminSections.All)
            {
                if (!allowedSections.Contains(item.Key)) { continue; }
                var isActive = currentController == item.Controller;
                <a asp-area="Admin" asp-controller="@item.Controller" asp-action="Index"
                   class="flex items-center gap-3 px-3 py-2.5 rounded-sm text-sm transition-colors @(isActive ? "bg-neutral-800 text-white" : "text-neutral-400 hover:text-white hover:bg-neutral-800")">
                    <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="@item.Icon"/>
                    </svg>
                    @item.Label
                </a>
            }

            @* Roles & Permissions — full administrators only *@
            @if (isFullAdmin)
            {
                var rolesActive = currentController == "Roles";
                <a asp-area="Admin" asp-controller="Roles" asp-action="Index"
                   class="flex items-center gap-3 px-3 py-2.5 rounded-sm text-sm transition-colors @(rolesActive ? "bg-neutral-800 text-white" : "text-neutral-400 hover:text-white hover:bg-neutral-800")">
                    <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                    Roles &amp; Permissions
                </a>
                var reviewsActive = currentController == "Reviews";
                <a asp-area="Admin" asp-controller="Reviews" asp-action="Index"
                   class="flex items-center gap-3 px-3 py-2.5 rounded-sm text-sm transition-colors @(reviewsActive ? "bg-neutral-800 text-white" : "text-neutral-400 hover:text-white hover:bg-neutral-800")">
                    <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.196-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.783-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"/>
                    </svg>
                    Reviews
                </a>
                var seoActive = currentController == "Seo";
                <a asp-area="Admin" asp-controller="Seo" asp-action="Index"
                   class="flex items-center gap-3 px-3 py-2.5 rounded-sm text-sm transition-colors @(seoActive ? "bg-neutral-800 text-white" : "text-neutral-400 hover:text-white hover:bg-neutral-800")">
                    <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7 8h10M7 12h6m-6 8l-4-4V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2H9l-2 2z"/>
                    </svg>
                    SEO Descriptions
                </a>
                var giftCardsActive = currentController == "GiftCards";
                <a asp-area="Admin" asp-controller="GiftCards" asp-action="Index"
                   class="flex items-center gap-3 px-3 py-2.5 rounded-sm text-sm transition-colors @(giftCardsActive ? "bg-neutral-800 text-white" : "text-neutral-400 hover:text-white hover:bg-neutral-800")">
                    <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 11.25v8.25a1.5 1.5 0 01-1.5 1.5H5.25a1.5 1.5 0 01-1.5-1.5v-8.25M12 4.875A2.625 2.625 0 109.375 7.5H12m0-2.625V7.5m0-2.625A2.625 2.625 0 1114.625 7.5H12m0 0V21m-8.25-13.5h16.5a1.5 1.5 0 011.5 1.5v1.5a1.5 1.5 0 01-1.5 1.5H3.75a1.5 1.5 0 01-1.5-1.5V9a1.5 1.5 0 011.5-1.5z"/>
                    </svg>
                    Gift Cards
                </a>
                var journalActive = currentController == "Journal";
                <a asp-area="Admin" asp-controller="Journal" asp-action="Index"
                   class="flex items-center gap-3 px-3 py-2.5 rounded-sm text-sm transition-colors @(journalActive ? "bg-neutral-800 text-white" : "text-neutral-400 hover:text-white hover:bg-neutral-800")">
                    <svg class="w-4 h-4 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 6.042A8.967 8.967 0 006 3.75c-1.052 0-2.062.18-3 .512v14.25A8.987 8.987 0 016 18c2.305 0 4.408.867 6 2.292m0-14.25a8.966 8.966 0 016-2.292c1.052 0 2.062.18 3 .512v14.25A8.987 8.987 0 0018 18a8.967 8.967 0 00-6 2.292m0-14.25v14.25"/>
                    </svg>
                    Journal
                </a>
            }
        </nav>

        <!-- Bottom: storefront link + logout -->
        <div class="px-3 py-4 border-t border-neutral-800 space-y-1">
            <a asp-controller="Home" asp-action="Index" asp-area=""
               class="flex items-center gap-3 px-3 py-2.5 text-xs text-neutral-500 hover:text-white transition-colors">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
                </svg>
                View Storefront
            </a>
            <form asp-controller="Account" asp-action="Logout" asp-area="" method="post">
                @Html.AntiForgeryToken()
                <button type="submit"
                        class="flex items-center gap-3 px-3 py-2.5 w-full text-xs text-neutral-500 hover:text-white transition-colors text-left">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                    </svg>
                    Sign Out
                </button>
            </form>
        </div>
    </aside>

    <!-- ─── Main ─────────────────────────────────────────────────────────── -->
    <div class="flex-1 flex flex-col overflow-hidden">

        <!-- Top bar -->
        <header class="bg-white border-b border-neutral-100 px-8 py-4 flex items-center justify-between gap-4 flex-shrink-0">
            <h1 class="text-sm font-medium text-neutral-800 flex-shrink-0">@ViewData["Title"]</h1>
            <form method="get" asp-area="Admin" asp-controller="Search" asp-action="Index" class="relative flex-1 max-w-md hidden sm:block">
                <svg class="w-4 h-4 text-neutral-400 absolute left-3 top-1/2 -translate-y-1/2 pointer-events-none" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                </svg>
                <input type="search" name="q" value="@(ViewContext.RouteData.Values["controller"]?.ToString() == "Search" ? Context.Request.Query["q"].ToString() : "")"
                       placeholder="Search orders, customers, products…"
                       class="w-full pl-9 pr-3 py-2 text-sm bg-neutral-50 border border-neutral-200 rounded-sm focus:outline-none focus:border-neutral-400 focus:bg-white transition-colors" />
            </form>
            <div class="flex items-center gap-3 flex-shrink-0">
                @if (TempData["Success"] != null)
                {
                    <span class="text-xs text-emerald-700 bg-emerald-50 px-3 py-1.5 rounded-full">✓ @TempData["Success"]</span>
                }
                @if (TempData["Error"] != null)
                {
                    <span class="text-xs text-red-700 bg-red-50 px-3 py-1.5 rounded-full">@TempData["Error"]</span>
                }
                <a asp-controller="Account" asp-action="Security" asp-area=""
                   class="flex items-center gap-1.5 text-xs text-neutral-400 hover:text-neutral-700 transition-colors" title="Account security (2FA)">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 11c0 3.517-1.009 6.799-2.753 9.571m-3.44-2.04l.054-.09A13.916 13.916 0 008 11a4 4 0 118 0c0 1.017-.07 2.019-.203 3m-2.118 6.844A21.88 21.88 0 0015.171 17m3.839 1.132c.645-2.266.99-4.659.99-7.132A8 8 0 008 4.07M3 15.364c.64-1.319 1-2.8 1-4.364 0-1.457.39-2.823 1.07-4"/></svg>
                    @User.Identity?.Name
                </a>
            </div>
        </header>

        <!-- Page content -->
        <main class="flex-1 overflow-y-auto p-8">
            @RenderBody()
            <partial name="_DevCredit" />
        </main>
    </div>
</div>

<script src="~/js/app.js" asp-append-version="true"></script>
@await RenderSectionAsync("Scripts", required: false)
<script>
// Global: disable submit buttons on form submit to prevent double-submissions
document.addEventListener('submit', function (e) {
    const form = e.target;
    form.querySelectorAll('button[type="submit"]').forEach(btn => {
        if (btn.dataset.noLoading) return;
        const original = btn.textContent.trim();
        btn.disabled = true;
        btn.textContent = 'Saving…';
        // Re-enable after 8s as safety net
        setTimeout(() => { btn.disabled = false; btn.textContent = original; }, 8000);
    });
});
</script>
</body>
</html>
